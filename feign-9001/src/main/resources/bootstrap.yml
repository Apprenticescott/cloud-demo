server:
  port: 9001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
spring:
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8500

